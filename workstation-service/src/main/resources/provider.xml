<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 配置应用程序的名称，一般使用项目名 -->
    <dubbo:application name="dubbo-spring-provider"/>

    <!-- 指定注册中心 zookeeper 的地址 -->
    <dubbo:registry address="zookeeper://101.43.37.149:2181"/>

    <!-- 协议和开放的访问端口-->
    <dubbo:protocol name="dubbo" port="8888"/>

    <!-- 配置要暴露的端口-->
    <dubbo:service interface="com.hangzhou.service.UserService" ref="userService"/>

    <bean id="userService" class="com.hangzhou.service.impl.UserServiceImpl"/>

</beans>
